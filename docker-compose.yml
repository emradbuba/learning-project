networks:
  test-net:

volumes:
  test-vol:

services:
  amq-broker:
    build: .
    container_name: activemq-artemis
    ports:
      - "8161:8161"
      - "61616:61616"
    volumes:
      - type: volume
        source: test-vol
        target: /activemq-artemis